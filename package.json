{
  "name": "dribbble-shots",
  "version": "0.1.0",
  "description": "a simple application to view the most popular shots in dribbble",
  "main": "index.js",
  "scripts": {
    "test": "eslint . && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "author": "malbernaz <albernazmiguel@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "eslint": "^4.2.0",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-react": "^7.1.0",
    "if-env": "^1.0.0",
    "jest": "^20.0.4",
    "node-sass": "^4.5.3",
    "preact-cli": "^1.3.0",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "preact": "^8.2.0",
    "preact-compat": "^3.16.0",
    "preact-portal": "^1.1.2",
    "preact-router": "^2.5.4"
  },
  "repository": "https://github.com/malbernaz/dribbble-shots.git"
}
